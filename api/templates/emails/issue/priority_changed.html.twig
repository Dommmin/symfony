{% extends 'emails/base.html.twig' %}

{% block title %}Zmiana priorytetu zgłoszenia{% endblock %}

{% block header %}Zmiana priorytetu zgłoszenia{% endblock %}

{% block content %}
    <div class="space-y-4">
        <p class="text-gray-700">
            Priorytet Twojego zgłoszenia <span class="font-mono bg-gray-100 px-2 py-1 rounded">{{ issue_id }}</span> 
            został zmieniony na: <span class="font-semibold text-blue-600">{{ priority }}</span>
        </p>

        <div class="bg-gray-50 p-4 rounded-lg mt-4">
            <h2 class="text-lg font-semibold mb-2">Szczegóły zgłoszenia:</h2>
            <ul class="list-none space-y-2">
                <li><strong>Tytuł:</strong> {{ issue.title }}</li>
                <li><strong>Status:</strong> {{ issue.status.value }}</li>
                <li><strong>Priorytet:</strong> <span class="text-blue-600">{{ priority }}</span></li>
                {% if issue.technician %}
                    <li><strong>Przypisany technik:</strong> {{ issue.technician.firstName }} {{ issue.technician.lastName }}</li>
                {% endif %}
            </ul>
        </div>
    </div>
{% endblock %} 